/// <reference types="bun-types" />
export { B as BunSQLiteDatabase, D as DrizzleConfig, d as drizzle } from '../driver.d-95363f1a.js';
import { Database, Statement as Statement$1 } from 'bun:sqlite';
import { L as Logger } from '../logger.d-37185354.js';
import { y as Query } from '../column.d-8b137277.js';
import { S as SQLiteSession, a as SQLiteSyncDialect, b as SelectedFieldsOrdered, c as SQLiteTransactionConfig, d as SQLiteTransaction, P as PreparedQuery$1, e as PreparedQueryConfig$1 } from '../db.d-fde890b4.js';
import '../select.types.d-c3e86d45.js';
import '../migrator.js';

interface SQLiteBunSessionOptions {
    logger?: Logger;
}
type PreparedQueryConfig = Omit<PreparedQueryConfig$1, 'statement' | 'run'>;
type Statement = Statement$1<any>;
declare class SQLiteBunSession extends SQLiteSession<'sync', void> {
    private client;
    private logger;
    constructor(client: Database, dialect: SQLiteSyncDialect, options?: SQLiteBunSessionOptions);
    exec(query: string): void;
    prepareQuery<T extends Omit<PreparedQueryConfig, 'run'>>(query: Query, fields?: SelectedFieldsOrdered): PreparedQuery<T>;
    transaction<T>(transaction: (tx: SQLiteBunTransaction) => T, config?: SQLiteTransactionConfig): T;
}
declare class SQLiteBunTransaction extends SQLiteTransaction<'sync', void> {
    transaction<T>(transaction: (tx: SQLiteBunTransaction) => T): T;
}
declare class PreparedQuery<T extends PreparedQueryConfig = PreparedQueryConfig> extends PreparedQuery$1<{
    type: 'sync';
    run: void;
    all: T['all'];
    get: T['get'];
    values: T['values'];
}> {
    private stmt;
    private queryString;
    private params;
    private logger;
    private fields;
    constructor(stmt: Statement, queryString: string, params: unknown[], logger: Logger, fields: SelectedFieldsOrdered | undefined);
    run(placeholderValues?: Record<string, unknown>): void;
    all(placeholderValues?: Record<string, unknown>): T['all'];
    get(placeholderValues?: Record<string, unknown>): T['get'];
    values(placeholderValues?: Record<string, unknown>): T['values'];
}

export { PreparedQuery, SQLiteBunSession, SQLiteBunSessionOptions, SQLiteBunTransaction };
