export { B as BetterSQLite3Database, D as DrizzleConfig, d as drizzle } from '../driver.d-fd3bd023.js';
import { RunResult, Database, Statement } from 'better-sqlite3';
import { L as Logger } from '../logger.d-37185354.js';
import { y as Query } from '../column.d-8b137277.js';
import { S as SQLiteSession, a as SQLiteSyncDialect, b as SelectedFieldsOrdered, c as SQLiteTransactionConfig, d as SQLiteTransaction, P as PreparedQuery$1, e as PreparedQueryConfig$1 } from '../db.d-fde890b4.js';
import '../select.types.d-c3e86d45.js';
import '../migrator.js';

interface BetterSQLiteSessionOptions {
    logger?: Logger;
}
type PreparedQueryConfig = Omit<PreparedQueryConfig$1, 'statement' | 'run'>;
declare class BetterSQLiteSession extends SQLiteSession<'sync', RunResult> {
    private client;
    private logger;
    constructor(client: Database, dialect: SQLiteSyncDialect, options?: BetterSQLiteSessionOptions);
    prepareQuery<T extends Omit<PreparedQueryConfig, 'run'>>(query: Query, fields: SelectedFieldsOrdered | undefined): PreparedQuery<T>;
    transaction<T>(transaction: (tx: BetterSQLiteTransaction) => T, config?: SQLiteTransactionConfig): T;
}
declare class BetterSQLiteTransaction extends SQLiteTransaction<'sync', RunResult> {
    transaction<T>(transaction: (tx: BetterSQLiteTransaction) => T): T;
}
declare class PreparedQuery<T extends PreparedQueryConfig = PreparedQueryConfig> extends PreparedQuery$1<{
    type: 'sync';
    run: RunResult;
    all: T['all'];
    get: T['get'];
    values: T['values'];
}> {
    private stmt;
    private queryString;
    private params;
    private logger;
    private fields;
    constructor(stmt: Statement, queryString: string, params: unknown[], logger: Logger, fields: SelectedFieldsOrdered | undefined);
    run(placeholderValues?: Record<string, unknown>): RunResult;
    all(placeholderValues?: Record<string, unknown>): T['all'];
    get(placeholderValues?: Record<string, unknown>): T['get'];
    values(placeholderValues?: Record<string, unknown>): T['values'];
}

export { BetterSQLiteSession, BetterSQLiteSessionOptions, BetterSQLiteTransaction, PreparedQuery };
