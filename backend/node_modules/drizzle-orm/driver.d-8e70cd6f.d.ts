import { Database } from 'sql.js';
import { L as Logger } from './logger.d-37185354.js';
import { B as BaseSQLiteDatabase } from './db.d-fde890b4.js';

interface DrizzleConfig {
    logger?: boolean | Logger;
}
type SQLJsDatabase = BaseSQLiteDatabase<'sync', void>;
declare function drizzle(client: Database, config?: DrizzleConfig): SQLJsDatabase;

export { DrizzleConfig as D, SQLJsDatabase as S, drizzle as d };
