interface Logger {
    logQuery(query: string, params: unknown[]): void;
}
interface LogWriter {
    write(message: string): void;
}
declare class ConsoleLogWriter implements LogWriter {
    write(message: string): void;
}
declare class DefaultLogger implements Logger {
    readonly writer: LogWriter;
    constructor(config?: {
        writer: LogWriter;
    });
    logQuery(query: string, params: unknown[]): void;
}
declare class NoopLogger implements Logger {
    logQuery(): void;
}

export { ConsoleLogWriter as C, DefaultLogger as D, Logger as L, NoopLogger as N, LogWriter as a };
